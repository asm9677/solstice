import { FC } from "react";
import { Canvas } from "../Canvas_TMP";

const Main: FC = () => {
  return (
      <div className="w-full h-full ">
          <Canvas />
    </div>
  );
};

export default Main;
